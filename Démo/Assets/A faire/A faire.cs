using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Afaire : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /*
     *  Changer les layer dans le code (carte à superposer)
     * Trouver un moyen de faire une zone prédéfini ( zone d'un joueur )
     * 
    */ 
}
